COPY (

CREATE TABLE count_table(
  TERM INTEGER,
  COUNT INTEGER
);


SELECT COUNT(foo.UNITS) , TERM FROM (SELECT UNITS,TERM FROM seating_tbl WHERE UNITS = 1) AS foo
  GROUP BY foo.TERM
  ORDER BY foo.TERM
)

 TO STDOUT WITH CSV HEADER;
